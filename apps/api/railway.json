{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && npm ci && npm run build --workspace=@heya-pos/types && npm run build --workspace=@heya-pos/utils && cd apps/api && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "envVars": {
    "NODE_ENV": {
      "default": "production"
    },
    "PORT": {
      "default": "3000"
    }
  }
}