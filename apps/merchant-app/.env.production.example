# API Configuration
NEXT_PUBLIC_API_URL=https://your-api-domain.com/api

# Base URL for static assets (leave empty for relative paths)
NEXT_PUBLIC_BASE_URL=

# If your app is served from a subdirectory (e.g., /merchant)
# NEXT_PUBLIC_BASE_PATH=/merchant

# Demo Configuration
# IMPORTANT: Set to 'false' or remove entirely for production
NEXT_PUBLIC_SHOW_DEMO_CREDENTIALS=false

# Payment SDK Configuration
NEXT_PUBLIC_TYRO_ENV=production
NEXT_PUBLIC_TYRO_MERCHANT_ID=your-merchant-id